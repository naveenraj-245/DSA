class Solution {
    public int majorityElement(int[] arr) {
        int count =0;
        int candidate=arr[0];
        for(int n:arr){
            if(count==0) candidate=n;
            if(n==candidate) count++;
            else count--;
        }
        return candidate;
    }
}